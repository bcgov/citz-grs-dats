import { Typography } from "@mui/material";
import { ProcessFolderButton } from "@renderer/components";

export const HomePage = () => {
	return (
		<>
			<Typography>Home</Typography>
			<ProcessFolderButton />
		</>
	);
};
