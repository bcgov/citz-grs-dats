/**
 * @summary This is the auth Endpoint for DATS
 * @author Jlevesqu
 */
import express from "express";
import AuthController from "../controller/auth-controller";

const router = express.Router();

// router.route("/auth").get(AuthController.getAuth);

// router.route("/auth/callback").get(AuthController.getTransfers);
